# Contributing
Install stable Rust, Node 22, and pnpm 10. Run `cargo test --workspace` and `pnpm test && pnpm build` before opening a pull request. Keep networking and filesystem logic in `hyper-core`.
