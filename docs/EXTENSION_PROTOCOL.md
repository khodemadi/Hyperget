# Future extension protocol
No listener ships in v0.1. A future protocol must bind loopback only, authenticate with a rotatable random installation token, validate origins and schemas, cap payloads, maintain site allow/deny rules, and never accept arbitrary output paths from pages. Native Messaging is preferred.
