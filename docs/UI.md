# UI
The desktop uses a compact responsive list, keyboard-focusable rows, light/dark persistence, real polling, status filtering, and explicit non-destructive removal. Bandwidth limiting and browser-captured links are visibly outside this milestone.
