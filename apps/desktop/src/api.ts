import{invoke}from'@tauri-apps/api/core';import type{Download,GlobalStatus,Priority,Settings}from'./types';export const api={list:()=>invoke<Download[]>('list_downloads'),status:()=>invoke<GlobalStatus>('get_global_status'),settings:()=>invoke<Settings>('get_settings'),updateSettings:(settings:Settings)=>invoke('update_settings',{settings}),add:(url:string)=>invoke<string>('add_download',{request:{url,output:null,connections:8,start_immediately:true,checksum_sha256:null}}),start:(id:string)=>invoke('start_download',{id}),pause:(id:string)=>invoke('pause_download',{id}),resume:(id:string)=>invoke('resume_download',{id}),cancel:(id:string)=>invoke('cancel_download',{id}),remove:(id:string,deleteData:boolean)=>invoke('remove_download',{id,deleteData}),priority:(id:string,priority:Priority)=>invoke('set_download_priority',{id,priority}),top:(id:string)=>invoke('move_download_to_top',{id}),bottom:(id:string)=>invoke('move_download_to_bottom',{id}),reorder:(ids:string[])=>invoke('reorder_downloads',{ids}),startAll:()=>invoke('start_all'),pauseAll:()=>invoke('pause_all')};
