import{useEffect,useRef,useState,type ReactNode}from'react';import{X}from'lucide-react';import{api,type WildcardRange}from'./api';
export function Dialog({title,children,onClose,footer}:{title:string;children:ReactNode;onClose:()=>void;footer:ReactNode}){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const root=ref.current,focusable=root?.querySelectorAll<HTMLElement>('button,input,select,textarea,[tabindex]');focusable?.[0]?.focus();const key=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose();if(e.key==='Tab'&&focusable?.length){const first=focusable[0],last=focusable[focusable.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}};document.addEventListener('keydown',key);return()=>document.removeEventListener('keydown',key)},[onClose]);return <div className="dialog-backdrop" role="presentation"><div ref={ref} className="dialog" role="dialog" aria-modal="true" aria-labelledby="dialog-title"><header><h2 id="dialog-title">{title}</h2><button aria-label="Close" onClick={onClose}><X/></button></header><div className="dialog-body">{children}</div><div className="dialog-footer">{footer}</div></div></div>}
export const parseUrls=(text:string)=>[...new Set(text.split(/\s+/).filter(Boolean).filter(v=>{try{return['http:','https:'].includes(new URL(v).protocol)}catch{return false}}))];
export function AddDialog({initial,onClose,onAdded}:{initial:string;onClose:()=>void;onAdded:()=>void}){const[url,setUrl]=useState(initial),[connections,setConnections]=useState(8),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async(start:boolean)=>{setError('');try{new URL(url);if(url.includes('*'))return setError('Use Batch Download for wildcard URLs.');setBusy(true);await api.add(url,start,connections);onAdded();onClose()}catch(e){setError(String(e))}finally{setBusy(false)}};return <Dialog title="Add Download" onClose={onClose} footer={<><button onClick={onClose}>Cancel</button><button disabled={busy} onClick={()=>submit(false)}>Add to queue</button><button className="primary" disabled={busy} onClick={()=>submit(true)}>Download now</button></>}><label>URL<input value={url} onChange={e=>setUrl(e.target.value)}/></label><div className="form-grid"><label>Connections<input type="number" min="1" max="32" value={connections} onChange={e=>setConnections(Number(e.target.value))}/></label><label>Priority<select defaultValue="normal"><option>low</option><option>normal</option><option>high</option><option>critical</option></select></label></div><label>Optional SHA-256<input placeholder="Checksum"/></label>{error&&<p className="error">{error}</p>}</Dialog>}
export function MultipleDialog({urls,onClose,onAdded}:{urls:string[];onClose:()=>void;onAdded:()=>void}){const[selected,setSelected]=useState(urls);const submit=async(start:boolean)=>{await api.addBatch(selected,8,start);onAdded();onClose()};return <Dialog title="Multiple Downloads" onClose={onClose} footer={<><button onClick={onClose}>Cancel</button><button disabled={!selected.length} onClick={()=>submit(false)}>Add all to queue</button><button className="primary" disabled={!selected.length} onClick={()=>submit(true)}>Download all</button></>}><p>{selected.length} valid URLs detected</p><div className="url-list">{selected.map(url=><div key={url}><button aria-label="Remove URL" onClick={()=>setSelected(x=>x.filter(v=>v!==url))}><X/></button><span>{new URL(url).hostname}</span><code>{url}</code></div>)}</div></Dialog>}
export function BatchDialog({pattern,onClose,onAdded}:{pattern:string;onClose:()=>void;onAdded:()=>void}){const count=Math.min(2,pattern.split('*').length-1),[ranges,setRanges]=useState<WildcardRange[]>(Array.from({length:count},()=>({start:1,end:10,step:1,padding:0}))),[urls,setUrls]=useState<string[]>([]),[error,setError]=useState('');const update=(i:number,key:keyof WildcardRange,n:number)=>setRanges(v=>v.map((r,x)=>x===i?{...r,[key]:n}:r));const preview=async()=>{try{setUrls(await api.previewBatch(pattern,ranges));setError('')}catch(e){setError(String(e))}};const submit=async(start:boolean)=>{await api.addBatch(urls,8,start);onAdded();onClose()};return <Dialog title="Batch Download" onClose={onClose} footer={<><button onClick={onClose}>Cancel</button><button onClick={preview}>Generate preview</button><button disabled={!urls.length} onClick={()=>submit(false)}>Add all to queue</button><button className="primary" disabled={!urls.length} onClick={()=>submit(true)}>Download all</button></>}><label>URL pattern<input value={pattern} readOnly/></label>{ranges.map((r,i)=><fieldset key={i}><legend>Wildcard {i+1}</legend>{(['start','end','step','padding']as const).map(k=><label key={k}>{k}<input type="number" min={k==='step'?1:0} value={r[k]} onChange={e=>update(i,k,Number(e.target.value))}/></label>)}</fieldset>)}<p>Generated: {urls.length}{urls.length>500?' — large batch, review carefully':''}</p><div className="preview">{urls.slice(0,100).map((url,i)=><div key={url}><b>{i+1}</b><span>{url.split('/').pop()}</span><code>{url}</code></div>)}</div>{error&&<p className="error">{error}</p>}</Dialog>}
