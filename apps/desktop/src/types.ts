export type State = 'created'|'resolving'|'queued'|'connecting'|'downloading'|'pausing'|'paused'|'retry_waiting'|'merging'|'verifying'|'completed'|'failed'|'cancelled';
export type Priority = 'low'|'normal'|'high'|'critical';
export interface Download { id:string; url:string; final_url:string|null; filename:string; destination:string; state:State; queue_position:number; priority:Priority; start_immediately:boolean; per_download_speed_limit:number|null; total_bytes:number|null; downloaded_bytes:number; connection_count:number; created_at:string; started_at:string|null; completed_at:string|null; etag:string|null; last_modified:string|null; error:string|null }
export interface GlobalStatus { total:number; downloaded_bytes:number; known_total_bytes:number; percentage:number|null; combined_speed:number; active:number; queued:number; paused:number; completed:number; failed:number; unknown_size:number; active_connections:number; eta_seconds:number|null; active_filename:string|null; queue_execution_enabled:boolean }
export interface Settings { maximum_simultaneous_downloads:number; default_connections_per_file:number; default_retry_count:number; initial_retry_delay_seconds:number; maximum_retry_delay_seconds:number; auto_retry:boolean; auto_start_next:boolean; restore_unfinished_downloads:boolean; global_speed_limit_mode:string; global_speed_limit_bytes:number; default_priority:Priority; confirm_before_delete:boolean; theme:string; default_download_directory:string; ask_where_to_save:boolean; remember_last_directory:boolean; last_selected_directory:string; create_category_subfolders:boolean; wildcard_batch_behavior:string; wildcard_auto_start:boolean; quick_download_bar_expanded:boolean; duplicate_filename_behavior:string; queue_execution_enabled:boolean }
